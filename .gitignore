# Operator configuration lives in ~/.claude/sfx-live-update/, never in the repo.
# These patterns are belt-and-braces in case someone runs from a working copy.
**/config/projects.json
**/config/people.json

*.pyc
__pycache__/
payload*.json
